package com.wearewaes.techassignment.aaroncastro.scalableweb;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Class that acts as the Spring App start point to load the container
 * @since version 1.0.0
 */
@SpringBootApplication
public class ScalableWebApplication {

    public static void main(String[] args) {
        SpringApplication.run(ScalableWebApplication.class, args);
    }

}
